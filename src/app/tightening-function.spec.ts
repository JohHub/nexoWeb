import { TighteningFunction } from './tightening-function';

describe('TighteningFunction', () => {
  it('should create an instance', () => {
    expect(new TighteningFunction()).toBeTruthy();
  });
});
